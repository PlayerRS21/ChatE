services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - chate_storage:/app/backend/storage
      - chate_uploads:/app/backend/uploads
      - chate_db:/app/backend
    restart: unless-stopped

volumes:
  chate_storage:
  chate_uploads:
  chate_db:
